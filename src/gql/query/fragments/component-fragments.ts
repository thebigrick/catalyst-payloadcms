import componentsRegistry from '@thebigrick/catalyst-payloadcms/registry/components-registry';

const componentFragments = componentsRegistry.map((c) => c.fragment);

export default componentFragments;
