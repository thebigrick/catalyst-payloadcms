import containersRegistry from '@thebigrick/catalyst-payloadcms/registry/containers-registry';

const containerFragments = containersRegistry.map((c) => c.fragment);

export default containerFragments;
