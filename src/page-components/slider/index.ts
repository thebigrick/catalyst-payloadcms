import { PageComponentDefinition } from '@thebigrick/catalyst-payloadcms/types';

import Fragment from './fragment';
import Schema from './schema';

const PageComponent: PageComponentDefinition = {
  fragment: Fragment,
  schema: Schema,
};

export default PageComponent;
